# Environment Variables
PORT=5000
NODE_ENV=development

# MongoDB Connection
# Local MongoDB (requires MongoDB to be installed and running)
# MONGODB_URI=mongodb://localhost:27017/ubuntu-explorer

# MongoDB Atlas (cloud) - recommended for development
# Replace with your actual MongoDB Atlas connection string
MONGODB_URI=mongodb+srv://ubuntu-explorer:hackathon2024@cluster0.mongodb.net/ubuntu-explorer?retryWrites=true&w=majority

# JWT Secret (generate a secure random string for production)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRE=7d

# API Configuration
API_VERSION=v1
